import 'package:flutter/material.dart';

class SpeedTrackingScreen extends StatelessWidget {
  const SpeedTrackingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('speed'),
      ),
    );
  }
}
